// src/config.js
// Per USDA serve una API key (gratuita): https://fdc.nal.usda.gov/api-key-signup.html
// Per la demo puoi lasciare stringa vuota (il provider USDA verrà saltato)
export const CONFIG = {
  USDA_API_KEY: "",   // <-- metti qui la tua chiave quando la avrai
  NUTRITION_CACHE_DAYS: 30,
};
